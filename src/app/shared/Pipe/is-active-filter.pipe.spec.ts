import { IsActiveFilterPipe } from './is-active-filter.pipe';

describe('IsActiveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IsActiveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
