import { SortByCreatedAtPipePipe } from './sort-by-created-at-pipe.pipe';

describe('SortByCreatedAtPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCreatedAtPipePipe();
    expect(pipe).toBeTruthy();
  });
});
